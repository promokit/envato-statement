export enum OrderTypes {
  Sale = 'Sale',
}

export enum Periods {
  Today = 'today',
  Yesterday = 'yesterday',
  CurrentWeek = 'currentweek',
  PreviousWeek = 'previousweek',
}

export enum Toggler {
  Show = 'show',
  Hide = 'hide',
}
